name: Build Vocal OS Public Alpha 0.2

on:
  pull_request:
    branches: [main]

jobs:
  build:
    if: github.head_ref == 'vocalos-public-alpha-v02'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Reconstruct and verify source
        run: |
          cat \
            vocalos-v02-parts/00 \
            vocalos-v02-parts/01 \
            vocalos-v02-parts/02 \
            vocalos-v02-parts/03a \
            vocalos-v02-parts/03b \
            vocalos-v02-parts/04a \
            vocalos-v02-parts/04b00 \
            vocalos-v02-parts/04b01 \
            vocalos-v02-parts/04b02 \
            vocalos-v02-parts/04b03 \
            vocalos-v02-parts/05a00 \
            vocalos-v02-parts/05a01 \
            vocalos-v02-parts/05a02 \
            vocalos-v02-parts/05a03 \
            vocalos-v02-parts/05b00 \
            vocalos-v02-parts/05b01 \
            vocalos-v02-parts/05b02 \
            vocalos-v02-parts/05b03 \
            > Vocal-OS-public-alpha-0.2-source.tar.gz
          echo "d309fbcaae620570867b12da62a9d6752c23a980f5ccffe3e9adb988e5ceff7c  Vocal-OS-public-alpha-0.2-source.tar.gz" | sha256sum -c -
          tar -xzf Vocal-OS-public-alpha-0.2-source.tar.gz
          test "$(grep -RIl 'ARKA\|com.arka' vocalos-public-alpha-v02 | wc -l)" = "0"
          grep -q "targetSdk 36" vocalos-public-alpha-v02/app/build.gradle
          grep -q "trainerCanvas" vocalos-public-alpha-v02/app/src/main/assets/index.html
          grep -q "alpha02" vocalos-public-alpha-v02/app/build.gradle
          grep -q "confidence" vocalos-public-alpha-v02/app/src/main/java/com/vocalos/trainer/MainActivity.java

      - name: Static web QA
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree as ET
          import re, subprocess, tempfile
          root=Path('vocalos-public-alpha-v02')
          for p in root.rglob('*.xml'):
              ET.parse(p)
          html=(root/'app/src/main/assets/index.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script[^>]*>(.*?)</script>', html, re.S|re.I)
          for i,s in enumerate(scripts):
              f=Path(tempfile.gettempdir())/f'vocalos-inline-{i}.js'
              f.write_text(s, encoding='utf-8')
              subprocess.run(['node','--check',str(f)], check=True)
          ids=re.findall(r'\bid=["\']([^"\']+)["\']', html)
          assert len(ids)==len(set(ids)), 'duplicate DOM ids'
          assert 'trainerCanvas' in ids
          print('Static QA OK:', len(scripts), 'scripts,', len(ids), 'unique ids')
          PY

      - name: Build API 36 APK and App Bundle
        run: gradle -p vocalos-public-alpha-v02 :app:assembleDebug :app:bundleDebug --stacktrace

      - name: Artifact smoke checks
        run: |
          APK=vocalos-public-alpha-v02/app/build/outputs/apk/debug/app-debug.apk
          AAB=vocalos-public-alpha-v02/app/build/outputs/bundle/debug/app-debug.aab
          unzip -t "$APK" >/dev/null
          unzip -t "$AAB" >/dev/null
          unzip -l "$APK" | grep -q AndroidManifest.xml
          unzip -l "$APK" | grep -q classes.dex
          unzip -l "$AAB" | grep -q 'base/manifest/AndroidManifest.xml'
          unzip -l "$AAB" | grep -q 'base/dex/classes.dex'
          sha256sum "$APK" "$AAB"

      - name: Upload APK and AAB
        uses: actions/upload-artifact@v4
        with:
          name: Vocal-OS-public-alpha-0.2
          path: |
            vocalos-public-alpha-v02/app/build/outputs/apk/debug/app-debug.apk
            vocalos-public-alpha-v02/app/build/outputs/bundle/debug/app-debug.aab
